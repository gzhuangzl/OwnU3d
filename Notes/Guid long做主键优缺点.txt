System.Guid 1.生成字符串，有38个字符长。比较占空间
	    2.生成byte[] 有16个字节长，转换成long也会丢失精度

	    如果涉及数据库迁移等，可以用这个生成UID，如果不涉及，可以用long自增替代，存储空间小
	    ，速度快

            注：System.Guid只是重复概率非常小，不保证完全不重复，在非常重要的地方最好用long自增，
	    如果涉及迁移等，单独弄一台服务器生成UID